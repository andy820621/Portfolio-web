[build]
  # 建置指令
  command = "npm run build"
  # 發佈目錄
  publish = "dist"

[build.environment]
  # Node.js 版本
  NODE_VERSION = "22.21.0"

# 防止 Netlify 使用舊的 Node 版本快取
[build.processing]
  skip_processing = false
